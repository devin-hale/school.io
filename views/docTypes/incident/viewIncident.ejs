<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <title>Incident Record</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="/classes">school.io</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/classes">Classes</a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  User: <%= user.fullName %>
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li><a class="dropdown-item" href="/user">Profile</a></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="/logout">Logout</a></li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      
        <div class="shadow-sm m-auto p-3 rounded" style="max-width: 900px;">
            <div class="row">
                <h2 class="col"><strong>Incident Report</strong></h2>
                <h3 class="col"><strong>Date:</strong><p><%= `${incident.date_of_occurence.getMonth()+1}-${incident.date_of_occurence.getDate()}-${incident.date_of_occurence.getFullYear()}` %></p></h3>
            </div>
            <div class="row">
                <h3 class="col"><strong>Teacher: </strong><p><%=incident.owner.fullName %></p></h3>
                <h3 class="col" style="word-wrap: unset;"><strong>Student: </strong><p><%= incident.students_involved[0].fullName %></p></h3>
            </div>
            <h4><strong>Subject: </strong><%=incident.subject %></h4>
            <div>
                <h4><strong>Description:</strong></h4>
                <p class="" style="min-height: 100px;"><%= incident.description %></p>
            </div>
            <div>
                <strong>Parent/Guardian Notified:</strong>
                <% if(incident.parentOrGuardian_notified) {%>
                    <p><em>Yes</em></p>
                <% } %>
                <% if(!incident.parentOrGuardian_notified) {%>
                    <p><em>No</em></p>
                <% } %>
            </div>
            <div>
                <strong>Escalated to Administration:</strong>
                <% if(incident.escalated) {%>
                    <p><em>Yes</em></p>
                <% } %>
                <% if(!incident.escalated) {%>
                    <p><em>No</em></p>
                <% } %>
            </div>
        </div>
        <div class="m-auto p-3 rounded d-flex justify-content-end" style="max-width: 900px;">
            <button type="button" class="btn btn-info">
                Save as PDF
            </button>
        </div>

      
    

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
</body>
</html>